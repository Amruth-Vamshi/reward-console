{
  "name": "walkinsole",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "cd packages/walkin-app && yarn start",
    "start-prod": "export NODE_ENV=prod && cd packages/walkin-app && yarn start",
    "test": "jest --config jest.config.js",
    "test:watch": "jest --config jest.config.js --watch",
    "coverage": "jest --config jest.config.js --coverage",
    "coverage:watch": "jest --config jest.config.js --coverage --watch",
    "build-dev": "export NODE_ENV=dev && cd packages/walkin-app && yarn build",
    "build": "cd packages/walkin-app && yarn build"
  },
  "devDependencies": {
    "babel-jest": "^24.8.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1",
    "coveralls": "^3.0.3",
    "eslint-plugin-react": "^7.13.0",
    "jest": "^24.8.0",
    "lerna": "^3.13.3",
    "react-test-renderer": "^16.8.6"
  },
  "workspaces": [
    "packages/*"
  ]
}